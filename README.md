# memcache-define-config

```shell
 git init
 git branch -m main

 git config user.name manhbv.giaminh
 git config user.email manhbv.giaminh@gmail.com
 git add .
 git commit -m 'memcache-define-config'

 git remote add origin git@github.com:ecsaas/memcache-define-config.git
 git push origin main
```
